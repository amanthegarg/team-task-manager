{
  "name": "team-task-manager-server",
  "version": "1.0.0",
  "description": "Team Task Manager - Express API server",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",

    "build": "npm install --prefix ../client && npm run build --prefix ../client && npx prisma generate",

    "start": "npx prisma db push --accept-data-loss && node server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "prisma": "^5.14.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}